name: third-party dependencies check

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      STATIC_DIR: hugegraph-loader/assembly/static
    steps:
      - name: Checkout source
        uses: actions/checkout@v3
      - name: Set up JDK 8
        uses: actions/setup-java@v3
        with:
          java-version: '8'
          distribution: 'adopt'
      - name: mvn install
        run: |
          mvn install:install-file -Dfile=$STATIC_DIR/lib/ojdbc8-12.2.0.1.jar -DgroupId=com.oracle -DartifactId=ojdbc8 -Dversion=12.2.0.1 -Dpackaging=jar
          mvn install -DskipTests=true
      - name: generate current dependencies
        run: |
          bash ${{ github.workspace }}/hugegraph-dist/scripts/dependency/regenerate_known_dependencies.sh current-dependencies.txt
      - name: check third dependencies
        run: |
          bash ${{ github.workspace }}/hugegraph-dist/scripts/dependency/check_dependencies.sh
